/** * universal-xml-converter-tab.js * Universal XML converter workbench with CL1 route package chain. */import { XML_PROFILES } from '../uxml/UxmlConstants.js';import { detectXmlProfile } from '../uxml/UxmlProfileDetector.js';import { normalizeXmlToUxml } from '../uxml/UxmlNormalizer.js';import { validateUxmlDocument } from '../uxml/UxmlValidationGate.js';import { buildUxmlFaceModel } from '../uxml/UxmlFaceModelBuilder.js';import { buildUxmlUniversalTopoGraph } from '../uxml/UxmlUniversalTopoGraphBuilder.js';import { buildUxmlRayTopoGraph } from '../uxml/UxmlRayTopoGraphBuilder.js';import { compareUxmlTopoGraphs } from '../uxml/UxmlTopoGraphComparator.js';import { decideUxmlTopologyAcceptance } from '../uxml/UxmlTopologyDecisionGate.js';import { UXML_ROUTE_TARGETS, createUxmlRouteHandoffPayload, summarizeUxmlRouteHandoff } from '../uxml/UxmlRouteHandoffPolicy.js';import { createUxmlCl1RoutePackage, summarizeUxmlCl1RoutePackage } from '../uxml/UxmlCl1RoutePackage.js';import { buildUxmlCl1PackageSnapshot, serializeUxmlCl1PackageSnapshot } from '../uxml/UxmlCl1PackageSnapshot.js';import { summarizeUxmlCl1SnapshotReplay, validateUxmlCl1SnapshotReplay } from '../uxml/UxmlCl1SnapshotReplayValidator.js';import { buildUxmlCl1WorkbenchSummary, summarizeUxmlCl1WorkbenchSummary } from '../uxml/UxmlCl1WorkbenchSummary.js';const SOURCE_TYPES = [  { value: 'AUTO', label: 'Auto detect' },  { value: 'EXISTING_XML', label: 'Existing XML / Standard XML' },  { value: 'INPUT_XML', label: 'InputXML' },  { value: 'UXML', label: 'UXML' },  { value: 'PCF', label: 'PCF' },  { value: 'PDF_TO_INPUTXML', label: 'PDF -> InputXML' },  { value: 'REV_TO_XML', label: 'REV -> XML' },  { value: 'JSON_TO_XML', label: 'JSON / staged JSON -> XML/InputXML' },  { value: 'TXT_TO_XML', label: 'Attribute TXT -> XML' },];const PIPELINE_STAGES = [  { id: 'source', title: '1. Source Intake', description: 'Load PDF / REV / JSON / TXT / PCF / XML source.' },  { id: 'existing-converter', title: '2. Existing Converter Output', description: 'Use existing converter routes to produce InputXML or Standard XML.' },  { id: 'uxml', title: '3. UXML Normalization', description: 'Normalize InputXML / Standard XML / UXML into the Universal XML contract.' },  { id: 'validation', title: '4. UXML Validation', description: 'Validate UXML structure, anchors, bore, branches, supports and loss contract.' },  { id: 'face-model', title: '5. Pre-Topology Face Model', description: 'Emit component/fitting faces for RayTopoBuilder before final topology.' },  { id: 'universal-topology', title: '6. UniversalTopoGraph', description: 'Build source-faithful topology graph from UXML faces.' },  { id: 'ray-topology', title: '7. RayTopoGraph', description: 'Run legacy-inspired Ray topology as independent benchmark/oracle.' },  { id: 'comparison', title: '8. Topology Comparison', description: 'Compare UniversalTopoGraph and RayTopoGraph evidence.' },  { id: 'decision-gate', title: '9. Decision Gate', description: 'Convert comparator evidence into accepted/manual/rejected topology decisions.' },  { id: 'route-handoff', title: '10. Route Handoff Policy', description: 'Decide what downstream route may receive accepted topology evidence.' },  { id: 'cl1-package', title: '11. CL1 Route Package', description: 'Create deterministic downstream route payload metadata without emitting PCF.' },  { id: 'cl1-snapshot', title: '12. CL1 Snapshot JSON', description: 'Create deterministic debug/replay JSON snapshot from the CL1 package.' },  { id: 'cl1-replay', title: '13. CL1 Replay Validator', description: 'Validate saved CL1 snapshot structure for debug/replay readiness.' },  { id: 'cl1-summary', title: '14. CL1 QA Summary', description: 'One-screen status summary for decision, route handoff, CL1 package and replay validation.' },  { id: 'outputs', title: '15. Route Targets', description: 'Target routes such as Extract PCF, GLB, 2D, InputXML or CII.', deferred: false },  { id: 'masters', title: '16. Masters by Target Route', description: 'Masters are handled by the downstream route. JSON/RVM -> PCF uses the existing legacy master route.', deferred: false },];function esc(value) {  return String(value ?? '').replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;').replaceAll('"', '&quot;').replaceAll("'", '&#039;');}function count(value) { return Number.isFinite(Number(value)) ? Number(value) : 0; }function reportPass(report) { return !!(report && (report.pass ?? report.ok ?? report.ready ?? report.replayReady)); }function summarizeReport(report) {  if (!report) return { pass: false, label: 'Not run', rows: [] };  const rows = [];  const stats = report.stats || report.summary || {};  for (const [k, v] of Object.entries(stats)) if (typeof v !== 'object') rows.push([k, v]);  if (report.profile) rows.unshift(['profile', report.profile]);  if (report.confidence) rows.unshift(['confidence', report.confidence]);  if (report.schema) rows.unshift(['schema', report.schema]);  return { pass: reportPass(report), label: reportPass(report) ? 'Ready' : 'Review', rows };}function createInitialState() {  return {    sourceFile: null, sourceText: '', selectedSourceType: 'AUTO', detectedSourceType: 'AUTO', activePanel: 'source',    status: { kind: 'info', message: 'Universal XML Converter tab is ready.' },    pipeline: { profileReport: null, normalizerResult: null, uxml: null, validationReport: null, faceModel: null, universalGraph: null, rayGraph: null, comparison: null, topologyDecision: null, routeHandoff: null, cl1RoutePackage: null, cl1Snapshot: null, cl1ReplayValidation: null, cl1WorkbenchSummary: null },    reports: { source: null, 'existing-converter': null, uxml: null, validation: null, 'face-model': null, 'universal-topology': null, 'ray-topology': null, comparison: null, 'decision-gate': null, 'route-handoff': null, 'cl1-package': null, 'cl1-snapshot': null, 'cl1-replay': null, 'cl1-summary': null, outputs: null, masters: null },  };}function sourceTypeFromProfile(profile) {  if (profile === XML_PROFILES.UXML) return 'UXML';  if (profile === XML_PROFILES.INPUT_XML) return 'INPUT_XML';  if (profile === XML_PROFILES.STANDARD_XML || profile === XML_PROFILES.BENCHMARK_XML) return 'EXISTING_XML';  return 'AUTO';}function extensionFallbackSourceType(fileName = '', text = '') {  const name = String(fileName || '').toLowerCase();  const trimmed = String(text || '').trimStart();  if (name.endsWith('.pcf')) return 'PCF';  if (name.endsWith('.pdf')) return 'PDF_TO_INPUTXML';  if (name.endsWith('.rev') || name.endsWith('.rvm')) return 'REV_TO_XML';  if (name.endsWith('.json') || trimmed.startsWith('{') || trimmed.startsWith('[')) return 'JSON_TO_XML';  if (name.endsWith('.txt')) return 'TXT_TO_XML';  return 'AUTO';}export function detectSourceType(fileName = '', text = '') {  const profile = detectXmlProfile(text, { fileName });  if (profile.isKnownProfile) return sourceTypeFromProfile(profile.profile);  if (String(fileName || '').toLowerCase().includes('input') && String(fileName || '').toLowerCase().endsWith('.xml')) return 'INPUT_XML';  return extensionFallbackSourceType(fileName, text);}function stageReport(stageId, report) { return { pass: reportPass(report), schema: report?.schema || '', summary: report?.summary || report?.stats || {}, report, stageId }; }function ensureXmlSource(state) {  const selected = state.selectedSourceType === 'AUTO' ? state.detectedSourceType : state.selectedSourceType;  if (!state.sourceText.trim()) throw new Error('Load XML/InputXML/UXML source text before running the UXML pipeline.');  if (!['AUTO', 'UXML', 'INPUT_XML', 'EXISTING_XML'].includes(selected)) throw new Error(`${selected} must go through the existing converter bridge before UXML normalization.`);}export function runUniversalXmlPipelineFromText(text, options = {}) {  const sourceName = options.sourceName || 'inline.xml';  const state = createInitialState();  state.sourceFile = { name: sourceName, size: String(text ?? '').length, type: 'text/xml', lastModified: null };  state.sourceText = String(text ?? '');  state.selectedSourceType = options.selectedSourceType || 'AUTO';  state.detectedSourceType = detectSourceType(sourceName, state.sourceText);  state.reports.source = { pass: true, fileName: sourceName, detectedSourceType: state.detectedSourceType };  ['detect-profile', 'convert-uxml', 'validate-uxml', 'build-face-model', 'build-universal-topology', 'build-ray-topology', 'compare-topology', 'run-decision-gate', 'run-route-handoff', 'run-cl1-package', 'run-cl1-snapshot', 'run-cl1-replay', 'run-cl1-summary'].forEach(action => runPipelineAction(state, action));  return state;}export function runPipelineAction(state, action) {  if (action === 'detect-profile') {    const profileReport = detectXmlProfile(state.sourceText, { fileName: state.sourceFile?.name || '', selectedSourceType: state.selectedSourceType });    state.pipeline.profileReport = profileReport;    state.detectedSourceType = profileReport.isKnownProfile ? sourceTypeFromProfile(profileReport.profile) : extensionFallbackSourceType(state.sourceFile?.name, state.sourceText);    state.reports.source = { pass: profileReport.isXml && profileReport.isKnownProfile, profile: profileReport.profile, confidence: profileReport.confidence, summary: profileReport.stats, report: profileReport };    state.status = profileReport.shouldBlockTopologyBuild ? { kind: 'warn', message: `Profile requires review: ${profileReport.blockers.join(', ') || profileReport.profile}` } : { kind: 'ok', message: `Detected XML profile: ${profileReport.profile}.` };    return profileReport;  }  if (action === 'convert-uxml') {    ensureXmlSource(state);    const profileReport = state.pipeline.profileReport || runPipelineAction(state, 'detect-profile');    const result = normalizeXmlToUxml(state.sourceText, { name: state.sourceFile?.name || '', fileName: state.sourceFile?.name || '', selectedSourceType: state.selectedSourceType, profileReport });    state.pipeline.normalizerResult = result; state.pipeline.uxml = result.uxml; state.reports.uxml = stageReport('uxml', result);    state.status = result.ok ? { kind: 'ok', message: `UXML normalization complete. Components=${count(result.stats?.componentCount)}, Anchors=${count(result.stats?.anchorCount)}.` } : { kind: 'error', message: 'UXML normalization blocked. Review diagnostics/loss contract.' };    return result;  }  if (action === 'validate-uxml') { if (!state.pipeline.uxml) runPipelineAction(state, 'convert-uxml'); const report = validateUxmlDocument(state.pipeline.uxml); state.pipeline.validationReport = report; state.reports.validation = stageReport('validation', report); state.status = report.ready ? { kind: 'ok', message: 'UXML validation passed.' } : { kind: 'warn', message: `UXML validation needs review. Blockers=${count(report.stats?.blockerCount)}.` }; return report; }  if (action === 'build-face-model') { if (!state.pipeline.uxml) runPipelineAction(state, 'convert-uxml'); const model = buildUxmlFaceModel(state.pipeline.uxml, { allowPartial: true }); state.pipeline.faceModel = model; state.reports['face-model'] = stageReport('face-model', model); state.status = model.ok ? { kind: 'ok', message: `Face model built. Faces=${count(model.summary?.faceCount)}.` } : { kind: 'warn', message: 'Face model built with warnings/blockers.' }; return model; }  if (action === 'build-universal-topology') { if (!state.pipeline.uxml) runPipelineAction(state, 'convert-uxml'); if (!state.pipeline.faceModel) runPipelineAction(state, 'build-face-model'); const graph = buildUxmlUniversalTopoGraph(state.pipeline.uxml, { faceModel: state.pipeline.faceModel, allowPartialFaceModel: true, allowBlockedFaceModel: true }); state.pipeline.universalGraph = graph; state.reports['universal-topology'] = stageReport('universal-topology', graph); state.status = graph.ok ? { kind: 'ok', message: `UniversalTopoGraph built. Edges=${count(graph.summary?.edgeCount)}.` } : { kind: 'warn', message: `UniversalTopoGraph built with disconnected=${count(graph.summary?.disconnectedCount)}.` }; return graph; }  if (action === 'build-ray-topology') { if (!state.pipeline.uxml) runPipelineAction(state, 'convert-uxml'); if (!state.pipeline.faceModel) runPipelineAction(state, 'build-face-model'); if (!state.pipeline.universalGraph) runPipelineAction(state, 'build-universal-topology'); const graph = buildUxmlRayTopoGraph(state.pipeline.uxml, { faceModel: state.pipeline.faceModel, universalGraph: state.pipeline.universalGraph, allowPartialFaceModel: true, allowBlockedFaceModel: true }); state.pipeline.rayGraph = graph; state.reports['ray-topology'] = stageReport('ray-topology', graph); state.status = graph.ok ? { kind: 'ok', message: `RayTopoGraph built. Candidates=${count(graph.summary?.rayCandidateCount)}.` } : { kind: 'warn', message: 'RayTopoGraph built with review items.' }; return graph; }  if (action === 'compare-topology') { if (!state.pipeline.universalGraph) runPipelineAction(state, 'build-universal-topology'); if (!state.pipeline.rayGraph) runPipelineAction(state, 'build-ray-topology'); const comparison = compareUxmlTopoGraphs(state.pipeline.uxml, { universalGraph: state.pipeline.universalGraph, rayGraph: state.pipeline.rayGraph, allowBlockedGraphs: true }); state.pipeline.comparison = comparison; state.reports.comparison = stageReport('comparison', comparison); state.status = comparison.ok ? { kind: 'ok', message: `Topology comparison complete. Promotions=${count(comparison.summary?.promotionCandidateCount)}, Manual=${count(comparison.summary?.manualReviewCount)}.` } : { kind: 'warn', message: 'Topology comparison needs review.' }; return comparison; }  if (action === 'run-decision-gate') { if (!state.pipeline.comparison) runPipelineAction(state, 'compare-topology'); const topologyDecision = decideUxmlTopologyAcceptance(state.pipeline.uxml, { comparison: state.pipeline.comparison, allowPartialExport: true, acceptUniversalOnly: true, allowSafeRayPromotions: true, allowFaceProximityPromotions: false, maxPromotionDistanceAlongRayMm: 500, maxPromotionPerpendicularMissMm: 12 }); state.pipeline.topologyDecision = topologyDecision; state.reports['decision-gate'] = stageReport('decision-gate', topologyDecision); state.status = topologyDecision.outputBridgeReady ? { kind: topologyDecision.exportAllowed ? 'ok' : 'warn', message: `Decision gate complete. Accepted=${count(topologyDecision.summary?.acceptedConnectionCount)}, Manual=${count(topologyDecision.summary?.manualReviewCount)}, Unresolved=${count(topologyDecision.summary?.unresolvedCount)}.` } : { kind: 'warn', message: 'Decision gate complete, but output bridge is not ready.' }; return topologyDecision; }  if (action === 'run-route-handoff') { if (!state.pipeline.topologyDecision) runPipelineAction(state, 'run-decision-gate'); const routeHandoff = createUxmlRouteHandoffPayload({ targetRoute: UXML_ROUTE_TARGETS.DIAGNOSTICS_ONLY, uxml: state.pipeline.uxml, topologyDecision: state.pipeline.topologyDecision, acceptedTopologyHandoff: null, diagnostics: state.pipeline.uxml?.diagnostics || [], lossContract: state.pipeline.uxml?.lossContract || [], allowPartialExport: true }); state.pipeline.routeHandoff = routeHandoff; state.reports['route-handoff'] = stageReport('route-handoff', routeHandoff.policy); state.status = { kind: routeHandoff.allowed ? 'ok' : 'warn', message: summarizeUxmlRouteHandoff(routeHandoff.policy) }; return routeHandoff; }  if (action === 'run-cl1-package') { if (!state.pipeline.routeHandoff) runPipelineAction(state, 'run-route-handoff'); const cl1RoutePackage = createUxmlCl1RoutePackage({ targetRoute: UXML_ROUTE_TARGETS.EXTRACT_PCF_LEGACY, uxml: state.pipeline.uxml, topologyDecision: state.pipeline.topologyDecision, acceptedTopologyHandoff: state.pipeline.routeHandoff, diagnostics: state.pipeline.uxml?.diagnostics || [], lossContract: state.pipeline.uxml?.lossContract || [], allowPartialExport: true, sourceInfo: { sourceFile: state.sourceFile?.name || '', selectedSourceType: state.selectedSourceType, detectedSourceType: state.detectedSourceType, profile: state.pipeline.profileReport?.profile || '' } }); state.pipeline.cl1RoutePackage = cl1RoutePackage; state.reports['cl1-package'] = stageReport('cl1-package', cl1RoutePackage); state.status = { kind: cl1RoutePackage.allowed ? 'ok' : 'warn', message: summarizeUxmlCl1RoutePackage(cl1RoutePackage) }; return cl1RoutePackage; }  if (action === 'run-cl1-snapshot') { if (!state.pipeline.cl1RoutePackage) runPipelineAction(state, 'run-cl1-package'); const cl1Snapshot = buildUxmlCl1PackageSnapshot(state.pipeline.cl1RoutePackage, { includePayload: false, includeDiagnostics: true, includeLossContract: true }); state.pipeline.cl1Snapshot = cl1Snapshot; state.reports['cl1-snapshot'] = stageReport('cl1-snapshot', cl1Snapshot); state.status = { kind: 'ok', message: `CL1 snapshot ready: ${cl1Snapshot.snapshotId}. Debug JSON only; no PCF or masters generated.` }; return cl1Snapshot; }  if (action === 'run-cl1-replay') { if (!state.pipeline.cl1Snapshot) runPipelineAction(state, 'run-cl1-snapshot'); const cl1ReplayValidation = validateUxmlCl1SnapshotReplay(state.pipeline.cl1Snapshot, { requirePayloadForReplay: false }); state.pipeline.cl1ReplayValidation = cl1ReplayValidation; state.reports['cl1-replay'] = stageReport('cl1-replay', cl1ReplayValidation); state.status = { kind: cl1ReplayValidation.replayReady ? 'ok' : 'warn', message: summarizeUxmlCl1SnapshotReplay(cl1ReplayValidation) }; return cl1ReplayValidation; }  if (action === 'run-cl1-summary') { if (!state.pipeline.cl1ReplayValidation) runPipelineAction(state, 'run-cl1-replay'); const cl1WorkbenchSummary = buildUxmlCl1WorkbenchSummary({ topologyDecision: state.pipeline.topologyDecision, routeHandoff: state.pipeline.routeHandoff, cl1RoutePackage: state.pipeline.cl1RoutePackage, cl1Snapshot: state.pipeline.cl1Snapshot, cl1ReplayValidation: state.pipeline.cl1ReplayValidation }); state.pipeline.cl1WorkbenchSummary = cl1WorkbenchSummary; state.reports['cl1-summary'] = stageReport('cl1-summary', cl1WorkbenchSummary); state.status = { kind: cl1WorkbenchSummary.overallStatus === 'PASS' ? 'ok' : cl1WorkbenchSummary.overallStatus === 'WARN' ? 'warn' : 'error', message: summarizeUxmlCl1WorkbenchSummary(cl1WorkbenchSummary) }; return cl1WorkbenchSummary; }  if (action === 'run-full-pipeline') { ['detect-profile', 'convert-uxml', 'validate-uxml', 'build-face-model', 'build-universal-topology', 'build-ray-topology', 'compare-topology', 'run-decision-gate', 'run-route-handoff', 'run-cl1-package', 'run-cl1-snapshot', 'run-cl1-replay', 'run-cl1-summary'].forEach((step) => { if (step !== action) runPipelineAction(state, step); }); return state.pipeline.cl1WorkbenchSummary || state.pipeline.routeHandoff; }  throw new Error(`Unknown UXML action: ${action}`);}function renderStageCard(stage, state) {  const isActive = state.activePanel === stage.id; const report = state.reports?.[stage.id] || null; const deferred = stage.deferred === true; const pass = reportPass(report);  return `<button class="uxml-stage-card ${isActive ? 'is-active' : ''} ${deferred ? 'is-deferred' : ''}" data-uxml-panel="${esc(stage.id)}" type="button"><div class="uxml-stage-title">${esc(stage.title)}</div><div class="uxml-stage-description">${esc(stage.description)}</div><div class="uxml-stage-meta">${deferred ? '<span class="uxml-pill muted">Deferred</span>' : report ? `<span class="uxml-pill ${pass ? 'ok' : 'warn'}">${pass ? 'Ready' : 'Review'}</span>` : '<span class="uxml-pill muted">Not run</span>'}</div></button>`;}function sourceSummaryHtml(state) {  const file = state.sourceFile; if (!file) return '<div class="uxml-empty">No source loaded.</div>';  return `<div class="uxml-kv-grid"><div>File</div><div>${esc(file.name)}</div><div>Size</div><div>${esc(`${count(file.size)} B`)}</div><div>Selected source type</div><div>${esc(state.selectedSourceType)}</div><div>Detected source type</div><div>${esc(state.detectedSourceType)}</div><div>Characters loaded</div><div>${esc(state.sourceText.length)}</div></div>`;}function reportSummaryHtml(report, title = 'Report') {  if (!report) return '<div class="uxml-empty">Not run yet.</div>';  const summary = summarizeReport(report);  return `<div class="uxml-report-card"><div class="uxml-report-title">${esc(title)} <span class="uxml-pill ${summary.pass ? 'ok' : 'warn'}">${esc(summary.label)}</span></div><div class="uxml-kv-grid uxml-kv-compact">${summary.rows.slice(0, 36).map(([k, v]) => `<div>${esc(k)}</div><div>${esc(v)}</div>`).join('')}</div></div>`;}function panelHtml(state) {  const panel = state.activePanel; const p = state.pipeline;  if (panel === 'source') return `<section class="uxml-panel-section"><h3>Source Intake</h3><p>Load XML, InputXML, or UXML directly. For JSON/RVM -> PCF, use the RVM / JSON -> PCF Extract tab and select UXML topology mode.</p>${sourceSummaryHtml(state)}${reportSummaryHtml(p.profileReport, 'Profile Detection')}<div class="uxml-preview-block"><div class="uxml-preview-title">Source Preview</div><pre>${esc((state.sourceText || '').slice(0, 12000))}</pre></div></section>`;  if (panel === 'existing-converter') return `<section class="uxml-panel-section"><h3>Existing Converter / Route Bridge</h3><p>This tab is a standalone XML/InputXML/UXML topology workbench. It does not own the JSON/RVM -> PCF production export workflow.</p><div class="uxml-placeholder"><b>JSON/RVM -> PCF production path:</b><br>Open <b>RVM / JSON -> PCF Extract</b>, then select <code>Topology mode = UXML topology</code>. That route uses UXML only for topology generation/checking, then continues through the existing master resolution and existing PCF emitter.</div><div class="uxml-placeholder" style="margin-top:12px;"><b>Standalone XML path in this tab:</b><pre style="white-space:pre-wrap;margin:8px 0 0;">XML / InputXML / UXML\n  -> UXML normalization\n  -> Validation\n  -> Face model\n  -> UniversalTopoGraph\n  -> RayTopoGraph\n  -> Comparator\n  -> Decision gate\n  -> Route handoff\n  -> CL1 package\n  -> CL1 snapshot\n  -> CL1 replay\n  -> CL1 summary</pre></div></section>`;  if (panel === 'uxml') return `<section class="uxml-panel-section"><h3>UXML Normalization</h3>${reportSummaryHtml(p.uxml, 'UXML Result')}<div class="uxml-preview-block"><div class="uxml-preview-title">Normalized UXML</div><pre>${esc(JSON.stringify(p.uxml?.uxml || null, null, 2).slice(0, 24000))}</pre></div></section>`;  if (panel === 'validation') return `<section class="uxml-panel-section"><h3>UXML Validation</h3>${reportSummaryHtml(p.validationReport, 'Validation')}</section>`;  if (panel === 'face-model') return `<section class="uxml-panel-section"><h3>Face Model</h3>${reportSummaryHtml(p.faceModel, 'Face Model')}</section>`;  if (panel === 'universal-topology') return `<section class="uxml-panel-section"><h3>UniversalTopoGraph</h3>${reportSummaryHtml(p.universalGraph, 'UniversalTopoGraph')}</section>`;  if (panel === 'ray-topology') return `<section class="uxml-panel-section"><h3>RayTopoGraph</h3>${reportSummaryHtml(p.rayGraph, 'RayTopoGraph')}</section>`;  if (panel === 'comparison') return `<section class="uxml-panel-section"><h3>Topology Comparison</h3>${reportSummaryHtml(p.comparison, 'Comparison')}</section>`;  if (panel === 'decision-gate') return `<section class="uxml-panel-section"><h3>Decision Gate</h3>${reportSummaryHtml(p.topologyDecision, 'Decision Gate')}</section>`;  if (panel === 'route-handoff') return `<section class="uxml-panel-section"><h3>Route Handoff Policy</h3><p>This stage only prepares route-handoff decisions.</p>${p.routeHandoff ? `<div class="uxml-placeholder"><b>Route handoff:</b><br>${esc(summarizeUxmlRouteHandoff(p.routeHandoff.policy))}</div>` : '<div class="uxml-placeholder">Run decision gate, then route handoff.</div>'}<div class="uxml-placeholder" style="margin-top:12px;"><b>Route contract:</b><br>UXML mutates coordinates: NO<br>UXML applies fixes: NO<br>UXML emits PCF directly: NO<br>Masters resolved here: NO</div></section>`;  if (panel === 'cl1-package') return `<section class="uxml-panel-section"><h3>CL1 Route Package</h3><p>This route package is deterministic metadata only. It does not emit PCF, does not resolve masters, and does not mutate topology.</p>${p.cl1RoutePackage ? `<div class="uxml-kv-grid"><div><b>Schema</b><span>${esc(p.cl1RoutePackage.schema)}</span></div><div><b>Package ID</b><span>${esc(p.cl1RoutePackage.packageId)}</span></div><div><b>Target route</b><span>${esc(p.cl1RoutePackage.targetRoute)}</span></div><div><b>Allowed</b><span>${p.cl1RoutePackage.allowed ? 'YES' : 'NO'}</span></div><div><b>Components</b><span>${count(p.cl1RoutePackage.entityCounts?.componentCount)}</span></div><div><b>Accepted topology</b><span>${count(p.cl1RoutePackage.topologyCounts?.acceptedConnectionCount)}</span></div></div><div class="uxml-placeholder" style="margin-top:12px;"><b>Route contract:</b><br>Does not emit PCF: ${p.cl1RoutePackage.routeContract?.uxmlEmitsPcfDirectly ? 'YES' : 'NO'}<br>Does not resolve masters: YES<br>Does not mutate coordinates: ${p.cl1RoutePackage.routeContract?.uxmlMutatesCoordinates ? 'NO' : 'YES'}</div>` : '<div class="uxml-placeholder">Run route handoff, then build CL1 package.</div>'}</section>`;  if (panel === 'cl1-snapshot') return `<section class="uxml-panel-section"><h3>CL1 Snapshot JSON</h3><p>Creates a deterministic debug/replay JSON snapshot from the CL1 route package. This is not PCF export and does not resolve masters.</p>${p.cl1Snapshot ? `<div class="uxml-kv-grid"><div><b>Schema</b><span>${esc(p.cl1Snapshot.schema)}</span></div><div><b>Snapshot ID</b><span>${esc(p.cl1Snapshot.snapshotId)}</span></div><div><b>Package ID</b><span>${esc(p.cl1Snapshot.packageId)}</span></div><div><b>Target route</b><span>${esc(p.cl1Snapshot.targetRoute)}</span></div><div><b>Debug only</b><span>${p.cl1Snapshot.debugOnly ? 'YES' : 'NO'}</span></div><div><b>Payload included</b><span>${p.cl1Snapshot.payloadIncluded ? 'YES' : 'NO'}</span></div><div><b>Components</b><span>${count(p.cl1Snapshot.entityCounts?.componentCount)}</span></div><div><b>Accepted topology</b><span>${count(p.cl1Snapshot.topologyCounts?.acceptedConnectionCount)}</span></div></div><div class="uxml-placeholder" style="margin-top:12px;"><b>Snapshot contract:</b><br>PCF generated: ${p.cl1Snapshot.pcfGenerated ? 'YES' : 'NO'}<br>Masters resolved: ${p.cl1Snapshot.mastersResolved ? 'YES' : 'NO'}<br>Coordinates mutated: ${p.cl1Snapshot.coordinatesMutated ? 'YES' : 'NO'}<br>Fixes applied: ${p.cl1Snapshot.fixesApplied ? 'YES' : 'NO'}</div><div class="uxml-preview-block" style="margin-top:12px;"><div class="uxml-preview-title">Snapshot preview</div><pre>${esc(serializeUxmlCl1PackageSnapshot(p.cl1Snapshot))}</pre></div>` : '<div class="uxml-placeholder">Run route handoff, then build CL1 snapshot.</div>'}</section>`;  if (panel === 'cl1-replay') return `<section class="uxml-panel-section"><h3>CL1 Replay Validator</h3><p>Validates a CL1 snapshot for debug/replay readiness. This does not parse XML, rebuild topology, emit PCF, resolve masters, mutate coordinates, or apply fixes.</p>${p.cl1ReplayValidation ? `<div class="uxml-kv-grid"><div><b>Schema</b><span>${esc(p.cl1ReplayValidation.schema)}</span></div><div><b>Replay ready</b><span>${p.cl1ReplayValidation.replayReady ? 'YES' : 'NO'}</span></div><div><b>Blocking issues</b><span>${count(p.cl1ReplayValidation.summary?.blockingIssueCount)}</span></div><div><b>Warnings</b><span>${count(p.cl1ReplayValidation.summary?.warningCount)}</span></div><div><b>Components</b><span>${count(p.cl1ReplayValidation.countSummary?.componentCount)}</span></div><div><b>Accepted topology</b><span>${count(p.cl1ReplayValidation.countSummary?.acceptedConnectionCount)}</span></div><div><b>Manual / rejected / unresolved</b><span>${count(p.cl1ReplayValidation.countSummary?.manualReviewCount)} / ${count(p.cl1ReplayValidation.countSummary?.rejectedCount)} / ${count(p.cl1ReplayValidation.countSummary?.unresolvedCount)}</span></div></div><div class="uxml-placeholder" style="margin-top:12px;"><b>Replay safety flags:</b><br>Debug only: ${p.cl1ReplayValidation.debugOnly ? 'YES' : 'NO'}<br>PCF generated: ${p.cl1ReplayValidation.pcfGenerated ? 'YES' : 'NO'}<br>Masters resolved: ${p.cl1ReplayValidation.mastersResolved ? 'YES' : 'NO'}<br>Coordinates mutated: ${p.cl1ReplayValidation.coordinatesMutated ? 'YES' : 'NO'}<br>Fixes applied: ${p.cl1ReplayValidation.fixesApplied ? 'YES' : 'NO'}</div>${p.cl1ReplayValidation.issues?.length ? `<div class="uxml-placeholder" style="margin-top:12px;"><b>Issues:</b><ul>${p.cl1ReplayValidation.issues.map((issue) => `<li><code>${esc(issue.code)}</code> - ${esc(issue.message)}</li>`).join('')}</ul></div>` : ''}` : '<div class="uxml-placeholder">Build CL1 snapshot, then validate replay readiness.</div>'}</section>`;  if (panel === 'cl1-summary') return `<section class="uxml-panel-section"><h3>CL1 QA Summary</h3><p>One-screen status for Decision Gate, Route Handoff, CL1 Route Package, Snapshot and Replay Validation. This is read-only QA status.</p>${p.cl1WorkbenchSummary ? `<div class="uxml-kv-grid"><div><b>Overall status</b><span>${esc(p.cl1WorkbenchSummary.overallStatus)}</span></div><div><b>Ready for route consumption</b><span>${p.cl1WorkbenchSummary.readyForRouteConsumption ? 'YES' : 'NO'}</span></div><div><b>Blocked / warning / not-run</b><span>${count(p.cl1WorkbenchSummary.blockedCount)} / ${count(p.cl1WorkbenchSummary.warningCount)} / ${count(p.cl1WorkbenchSummary.notRunCount)}</span></div><div><b>Components</b><span>${count(p.cl1WorkbenchSummary.counts?.componentCount)}</span></div><div><b>Accepted topology</b><span>${count(p.cl1WorkbenchSummary.counts?.acceptedConnectionCount)}</span></div></div><div class="uxml-placeholder" style="margin-top:12px;"><b>Safety summary:</b><br>PCF generated: ${p.cl1WorkbenchSummary.safety?.pcfGenerated ? 'YES' : 'NO'}<br>Masters resolved: ${p.cl1WorkbenchSummary.safety?.mastersResolved ? 'YES' : 'NO'}<br>Coordinates mutated: ${p.cl1WorkbenchSummary.safety?.coordinatesMutated ? 'YES' : 'NO'}<br>Fixes applied: ${p.cl1WorkbenchSummary.safety?.fixesApplied ? 'YES' : 'NO'}</div>` : '<div class="uxml-placeholder">Run CL1 replay validator, then build CL1 QA summary.</div>'}</section>`;  if (panel === 'masters') return `<section class="uxml-panel-section"><h3>Masters by Target Route</h3><div class="uxml-placeholder">Masters are owned by the downstream route. This tab only prepares topology and CL1 route evidence.</div></section>`;  return `<section class="uxml-panel-section"><h3>Unknown panel</h3><div class="uxml-placeholder">${esc(panel)}</div></section>`;}function canRunXmlActions(state) { const selected = state.selectedSourceType === 'AUTO' ? state.detectedSourceType : state.selectedSourceType; return !!state.sourceText.trim() && ['AUTO', 'UXML', 'INPUT_XML', 'EXISTING_XML'].includes(selected); }function render(container, state) {  const xmlReady = canRunXmlActions(state);  container.innerHTML = `<div class="uxml-tab"><header class="uxml-header"><div><h2>Universal XML Converter</h2><p>XML/InputXML/UXML topology workbench: source -> UXML -> validation -> face model -> UniversalTopoGraph + RayTopoGraph comparison -> route handoff -> CL1 package -> CL1 snapshot -> CL1 replay -> CL1 summary.</p></div><div class="uxml-header-badges"><span class="uxml-badge">Agent 09</span><span class="uxml-badge">Integrated Pipeline</span><span class="uxml-badge muted">Masters by target route</span></div></header><section class="uxml-toolbar"><label class="uxml-field"><span>Source type</span><select data-uxml-source-type>${SOURCE_TYPES.map((option) => `<option value="${esc(option.value)}" ${state.selectedSourceType === option.value ? 'selected' : ''}>${esc(option.label)}</option>`).join('')}</select></label><label class="uxml-file-btn">Load Source<input data-uxml-file-input type="file" /></label><button data-uxml-action="detect-profile" type="button">Detect Profile</button><button data-uxml-action="convert-uxml" type="button" ${xmlReady ? '' : 'disabled'}>Convert to UXML</button><button data-uxml-action="validate-uxml" type="button" ${state.pipeline.uxml ? '' : 'disabled'}>Validate UXML</button><button data-uxml-action="build-face-model" type="button" ${state.pipeline.uxml ? '' : 'disabled'}>Build Face Model</button><button data-uxml-action="build-universal-topology" type="button" ${state.pipeline.faceModel ? '' : 'disabled'}>Build UniversalTopoGraph</button><button data-uxml-action="build-ray-topology" type="button" ${state.pipeline.faceModel ? '' : 'disabled'}>Build RayTopoGraph</button><button data-uxml-action="compare-topology" type="button" ${state.pipeline.universalGraph && state.pipeline.rayGraph ? '' : 'disabled'}>Compare</button><button data-uxml-action="run-decision-gate" type="button" ${state.pipeline.comparison ? '' : 'disabled'}>Run decision gate</button><button data-uxml-action="run-route-handoff" type="button" ${state.pipeline.topologyDecision ? '' : 'disabled'}>Run route handoff</button><button data-uxml-action="run-cl1-package" type="button" ${state.pipeline.routeHandoff ? '' : 'disabled'}>Build CL1 package</button><button data-uxml-action="run-cl1-snapshot" type="button" ${state.pipeline.cl1RoutePackage ? '' : 'disabled'}>Build CL1 snapshot</button><button data-uxml-action="run-cl1-replay" type="button" ${state.pipeline.cl1Snapshot ? '' : 'disabled'}>Validate CL1 replay</button><button data-uxml-action="run-cl1-summary" type="button" ${state.pipeline.cl1ReplayValidation ? '' : 'disabled'}>Build CL1 QA summary</button><button data-uxml-action="run-full-pipeline" type="button" ${xmlReady ? '' : 'disabled'}>Run Full Pipeline</button><button data-uxml-action="export-summary" type="button">Export Summary JSON</button></section><div class="uxml-status ${statusClass(state.status.kind)}">${esc(state.status.message)}</div><main class="uxml-layout"><aside class="uxml-stages">${PIPELINE_STAGES.map((stage) => renderStageCard(stage, state)).join('')}</aside><section class="uxml-panel">${panelHtml(state)}</section></main></div>`;}function buildSummary(state) {  return {    schema: 'pcf-glb-viewer/universal-xml-converter-tab-summary/v2',    phase: 'Agent09',    generatedAt: new Date().toISOString(),    source: state.sourceFile ? { name: state.sourceFile.name, size: state.sourceFile.size, selectedSourceType: state.selectedSourceType, detectedSourceType: state.detectedSourceType, charactersLoaded: state.sourceText.length } : null,    reports: Object.fromEntries(Object.entries(state.reports).map(([key, value]) => [key, value ? { pass: reportPass(value), summary: value.summary || value.stats || null } : null])),    comparator: state.pipeline.comparison?.summary || null,    deferred: { existingConverterBridge: true, outputBridges: true, masters: true },  };}function downloadText(filename, content, mime = 'application/json') {  const blob = new Blob([content], { type: mime });  const url = URL.createObjectURL(blob);  const a = document.createElement('a');  a.href = url; a.download = filename; document.body.appendChild(a); a.click(); a.remove(); URL.revokeObjectURL(url);}function exportSummary(state) { downloadText('universal_xml_converter_agent09_summary.json', JSON.stringify(buildSummary(state), null, 2), 'application/json'); }function bindEvents(container, state) {  container.addEventListener('change', async (event) => {    const sourceSelect = event.target.closest('[data-uxml-source-type]');    if (sourceSelect) { state.selectedSourceType = sourceSelect.value || 'AUTO'; state.status = { kind: 'info', message: `Source type set to ${state.selectedSourceType}.` }; render(container, state); return; }    const fileInput = event.target.closest('[data-uxml-file-input]'); if (!fileInput) return;    const file = fileInput.files?.[0] || null;    if (!file) { state.status = { kind: 'warn', message: 'No file selected.' }; render(container, state); return; }    try {      const text = await file.text();      state.sourceFile = { name: file.name, size: file.size, type: file.type || '', lastModified: file.lastModified || null };      state.sourceText = text; state.detectedSourceType = detectSourceType(file.name, text); state.reports.source = { pass: true, fileName: file.name, size: file.size, detectedSourceType: state.detectedSourceType }; state.status = { kind: 'ok', message: `Loaded ${file.name}. Detected source type: ${state.detectedSourceType}.` };    } catch (error) { state.status = { kind: 'error', message: `Failed to read source file: ${error.message}` }; }    render(container, state);  });  container.addEventListener('click', (event) => {    const panelButton = event.target.closest('[data-uxml-panel]'); if (panelButton) { state.activePanel = panelButton.dataset.uxmlPanel || 'source'; render(container, state); return; }    const actionButton = event.target.closest('[data-uxml-action]'); if (!actionButton) return;    const action = actionButton.dataset.uxmlAction;    try {      if (action === 'export-summary') { exportSummary(state); state.status = { kind: 'ok', message: 'Universal XML Converter Agent09 summary exported.' }; }function bindEvents(container, state) {
  container.addEventListener('change', async (event) => {
    const sourceSelect = event.target.closest('[data-uxml-source-type]');
    if (sourceSelect) {
      state.selectedSourceType = sourceSelect.value || 'AUTO';
      state.status = { kind: 'info', message: `Source type set to ${state.selectedSourceType}.` };
      render(container, state);
      return;
    }

    const fileInput = event.target.closest('[data-uxml-file-input]');
    if (!fileInput) return;

    const file = fileInput.files?.[0] || null;
    if (!file) {
      state.status = { kind: 'warn', message: 'No file selected.' };
      render(container, state);
      return;
    }

    try {
      const text = await file.text();
      state.sourceFile = {
        name: file.name,
        size: file.size,
        type: file.type || '',
        lastModified: file.lastModified || null,
      };
      state.sourceText = text;
      state.detectedSourceType = detectSourceType(file.name, text);
      state.reports.source = {
        pass: true,
        fileName: file.name,
        size: file.size,
        detectedSourceType: state.detectedSourceType,
      };
      state.status = {
        kind: 'ok',
        message: `Loaded ${file.name}. Detected source type: ${state.detectedSourceType}.`,
      };
    } catch (error) {
      state.status = { kind: 'error', message: `Failed to read source file: ${error.message}` };
    }

    render(container, state);
  });

  container.addEventListener('click', (event) => {
    const panelButton = event.target.closest('[data-uxml-panel]');
    if (panelButton) {
      state.activePanel = panelButton.dataset.uxmlPanel || 'source';
      render(container, state);
      return;
    }

    const actionButton = event.target.closest('[data-uxml-action]');
    if (!actionButton) return;

    const action = actionButton.dataset.uxmlAction;

    try {
      if (action === 'export-summary') {
        exportSummary(state);
        state.status = { kind: 'ok', message: 'Universal XML Converter Agent09 summary exported.' };
      } else {
        runPipelineAction(state, action);
        if (action === 'detect-profile') state.activePanel = 'source';
        if (action === 'convert-uxml') state.activePanel = 'uxml';
        if (action === 'validate-uxml') state.activePanel = 'validation';
        if (action === 'build-face-model') state.activePanel = 'face-model';
        if (action === 'build-universal-topology') state.activePanel = 'universal-topology';
        if (action === 'build-ray-topology') state.activePanel = 'ray-topology';
        if (action === 'compare-topology') state.activePanel = 'comparison';
        if (action === 'run-decision-gate') state.activePanel = 'decision-gate';
        if (action === 'run-route-handoff') state.activePanel = 'route-handoff';
        if (action === 'run-cl1-package') state.activePanel = 'cl1-package';
        if (action === 'run-cl1-snapshot') state.activePanel = 'cl1-snapshot';
        if (action === 'run-cl1-replay') state.activePanel = 'cl1-replay';
        if (action === 'run-cl1-summary') state.activePanel = 'cl1-summary';
      }
    } catch (error) {
      state.status = { kind: 'error', message: error.message };
    }

    render(container, state);
  });
}

export function renderUniversalXmlConverterTab(container) {
  const state = createInitialState();

  render(container, state);
  bindEvents(container, state);

  return () => {
    container.innerHTML = '';
  };
}

export const _test = Object.freeze({
  createInitialState,
  summarizeReport,
  buildSummary,
  canRunXmlActions,
  PIPELINE_STAGES,
});